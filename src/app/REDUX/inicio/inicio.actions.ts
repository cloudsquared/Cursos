import { Action } from "@ngrx/store"

const aumentadorAction:Action={
    type: "AUMENTAR"  
  }
  
  const decrementadorAction:Action={
    type: "DECREMENTAR"  
  }
  
  const multiplicarAction:Action={
    type: "MULTIPLICAR" ,
  //  payload: 2
  }